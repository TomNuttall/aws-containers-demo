{
  "name": "demo_ecs",
  "version": "1.0.0",
  "author": "Tom Nuttall <tsnuttall@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "cd app && docker compose --profile dev up --build demo-ecs",
    "s3:start": "cd app && docker compose --profile dev up -d s3",
    "s3:stop": "cd app && docker compose down",
    "test": "cd app && docker compose --profile test up --build"
  }
}
