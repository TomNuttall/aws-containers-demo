{
  "name": "demo_ecs",
  "version": "1.0.0",
  "author": "Tom Nuttall <tsnuttall@gmail.com>",
  "license": "MIT",
  "scripts": {
    "docker:build": "docker build -t $npm_package_name:$npm_package_version ./app",
    "docker:run": "docker run --rm $npm_package_name:$npm_package_version"
  }
}
